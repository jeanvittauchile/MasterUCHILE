{
  "name": "masteruchile",
  "private": true,
  "workspaces": [
    "app",
    "server",
    "packages/shared"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=@masteruchile/shared",
    "dev:server": "npm run dev --workspace=server",
    "dev:app": "npm run web --workspace=app",
    "build:shared": "npm run build --workspace=@masteruchile/shared",
    "test": "npm run test --workspace=@masteruchile/shared --if-present && npm run test --workspace=server --if-present",
    "typecheck": "npm run typecheck --workspace=@masteruchile/shared && npm run typecheck --workspace=server && npm run typecheck --workspace=app",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
